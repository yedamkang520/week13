class Zebra(Animal) :
    def move(self,gird):
        neighbors = self.get_neighbors(grid,target =".")
        chosen_nei = randome.choice(neghbors)
        self.x,self.y = chosen_neighbor

class Lion (Animal)
    def move(self,grid)
        neighbors = 